# 增量更新：讓 CMoney 只抓最新一期

## 這是在解決什麼問題

原本每次更新，都要叫 CMoney 把**全部歷史**重抓一遍（周收 600 期、PB 600 期、月營收 120 期…），
又慢又沒必要——舊資料根本不會變。

改成：歷史存在本機的**資料倉**（`archive/` 底下 17 個 CSV，全部才 4MB），CMoney 只抓最新 1~2 期，
每次更新把新那幾期「覆蓋」進資料倉，網站要用的完整歷史一律從資料倉拿。

順帶解決 Excel 檔案 227MB 的問題：其中 `統整` 佔 88.5MB（格式被拉到 104 萬列，實際只用 272 列），
`FwdPE_周` 46MB、`預估明年EPS成長` 40MB、`加權Fwd.EPS` 24MB 是三張純公式表，
這三張現在已經改由 Python 重算，可以直接從 Excel 刪掉。四項加起來 198MB。

---

## 你要做的事（照順序）

### 第 1 步：建立歷史資料倉（只做一次）

**務必在改 CMoney 設定之前做**，不然歷史就抓不回來了。

雙擊 **`1_建立歷史資料倉_只需執行一次.bat`**，跑完會在專案資料夾出現 `archive/`。

跑完請**把 `archive/` 整個資料夾另外複製一份備份**（雲端硬碟、隨身碟都行）。
它沒有進 GitHub（裡面是 CMoney 付費資料），從此是你所有歷史的唯一底本。

### 第 2 步：確認網站資料正常

雙擊 **`refresh_and_publish.bat`**，跑完打開網站確認圖表和篩選器都正常。
這一步 Excel 還是完整的，只是驗證新流程走得通。

### 第 3 步：把 Excel 瘦身（建議，但可以晚點再做）

先**複製一份 Excel 備份**，然後：

1. **刪掉三張純公式表**：`FwdPE_周`、`加權Fwd.EPS`、`預估明年EPS成長`
   （這三張已經改由 Python 算，程式不會再讀它們）
2. **清掉 `統整` 的空白列**：選取第 300 列到最後一列 → 右鍵刪除 → 存檔
3. 存成 xlsm

### 第 4 步：把 CMoney 的 TOP N 改小

在 CMoney 增益集裡，把這幾張表的抓取期數改成 **TOP 2**（留 2 期當重疊保險，
CMoney 事後修正數字時才蓋得掉）：

| Sheet | 現在 | 改成 |
|---|---|---|
| 周收 | TOP 10 | TOP 2 |
| PB | TOP 120 | TOP 2 |
| 月收 | TOP 120 | TOP 2 |
| 月營收 | TOP 120 | TOP 2 |
| 今年Est.EPS | TOP 120 | TOP 2 |
| 明年Est.EPS | TOP 120 | TOP 2 |
| 預估今年EPS成長 | TOP 120 | TOP 2 |
| 法人持股 | TOP 250 | TOP 2 |
| 自營持股 / 投信持股 / 外資持股 | TOP 120 | TOP 2 |
| Bwd.EPS | TOP 8 | TOP 8（本來就短，不用改）|
| Margin | TOP 4 | TOP 4（不用改）|
| BVPS / payout / 統整 | TOP 1~3 | 不用改 |

改完存檔，然後再跑一次 `refresh_and_publish.bat` 確認網站數字沒變。

---

## 之後的日常流程

跟以前一樣，沒有變：

- 在裝 CMoney 的電腦：雙擊 **`update_from_cmoney.bat`**（更新 → 併進資料倉 → 重算 → 推上網）
- 自己手動更新完 Excel：雙擊 **`refresh_and_publish.bat`**

`refresh_data.py` 只要看到 `archive/` 存在，就會自動走資料倉這條路；
把 `archive/` 移走或改名，它會自動退回原本「直接讀 Excel」的模式。

---

## 這次改動驗證了什麼

改資料來源最怕數字悄悄跑掉，所以每一項都跟原本的 Excel 逐格比對過：

| 項目 | 比對量 | 結果 |
|---|---|---|
| 資料倉存取（寫進去再讀回來） | 197,107 格 | 完全一致 |
| Python 重算 加權Fwd.EPS | 47,700 格 | 完全一致 |
| Python 重算 FwdPE_周 | 154,554 格 | 完全一致 |
| Python 重算 預估明年EPS成長 | 22,300 格 | 完全一致 |
| 全部 265 檔的圖表資料 | 3,114,232 格 | 見下方說明 |
| 篩選器 9 個歷史統計欄位 × 265 檔 | 2,385 格 | 見下方說明 |

圖表與篩選器有差異的部分，逐一追查後確認**全部都是新版比較正確**，沒有任何一格是
「兩邊都有值但算出不同答案」：

1. **Margin 的 GPM / OPM 是修正了舊的 bug**。原本的程式在 Margin 那張三區塊的表上算錯欄位偏移，
   結果毛利率和營業利益率兩條線都畫成了「稅前純益率」。新版用 台泥 驗算：
   4Q GPM 19.95、4Q OPM 8.45，跟 `統整` V/W 欄自己算的完全吻合。**你網站上的 Margin 圖之前是錯的，現在對了。**
2. **`#DIV/0!` 不再流進資料**。原本今年預估 EPS 是 0 的股票，Excel 算出 `#DIV/0!` 字串會被原封不動寫進
   網站資料；新版直接給空值。
3. **歷史往前多了一段**。EPS 成長率原本受限於 Excel 那張表只有 120 期，新版從資料倉能算到 180 期。
4. **新光金(2888) 的 PE Band 少了 96 個 0**。它某段期間預估 EPS 是 0，舊版把band畫成 0（等於圖上拉一條到零的線），新版給空值。
5. 篩選器只有 3 檔有差（2395 研華、1560 中砂、2888 新光金），都是 `統整` 那幾列本來就是空白、
   Python 反而從資料倉算得出正確值。

---

## 萬一出問題

- **資料倉不見了 / 壞了**：把備份的 `archive/` 複製回來就好。真的沒有備份，就把 CMoney 的 TOP N
  調回原本的數字、抓一次完整歷史，再跑一次第 1 步重建。
- **想暫時回到舊做法**：把 `archive/` 改名（例如 `archive_off`），程式會自動退回直接讀 Excel。
  但這時 Excel 必須有完整歷史才算得對。
- **某一期數字怪怪的**：`archive/` 裡就是 CSV，可以直接用 Excel 打開，找到那一檔那一期改掉或清空。
